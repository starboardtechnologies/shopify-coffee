import { n, s, v } from "./node_modules/.pnpm/@xstate_fsm@2.0.0/node_modules/@xstate/fsm/es/index.mjs";
import { useCallback, useEffect, useLayoutEffect, useRef, useSyncExternalStore } from "react";
//#region src/useMachine.ts
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? useLayoutEffect : useEffect;
function useConstant(fn) {
	const ref = useRef();
	if (!ref.current) ref.current = { v: fn() };
	return ref.current.v;
}
function getServiceState(service) {
	let currentValue;
	service.subscribe((state) => currentValue = state).unsubscribe();
	return currentValue;
}
function useMachine(stateMachine, options) {
	const persistedStateRef = useRef();
	const [service, queue] = useConstant(() => {
		const eventQueue = [];
		const svc = v(s(stateMachine.config, options ? options : stateMachine._options));
		const originalSend = svc.send;
		svc.send = (event) => {
			if (svc.status === n.NotStarted) {
				eventQueue.push(event);
				return;
			}
			originalSend(event);
			persistedStateRef.current = svc.state;
		};
		return [svc, eventQueue];
	});
	useIsomorphicLayoutEffect(() => {
		if (options) service._machine._options = options;
	});
	const getSnapshot = useCallback(() => getServiceState(service), [service]);
	const storeSnapshot = useSyncExternalStore(useCallback((handleStoreChange) => {
		const { unsubscribe } = service.subscribe(handleStoreChange);
		return unsubscribe;
	}, [service]), getSnapshot, getSnapshot);
	useEffect(() => {
		service.start(persistedStateRef.current);
		queue.forEach(service.send);
		persistedStateRef.current = service.state;
		return () => {
			service.stop();
		};
	}, []);
	return [
		storeSnapshot,
		service.send,
		service
	];
}
//#endregion
export { useMachine };

//# sourceMappingURL=useMachine.mjs.map