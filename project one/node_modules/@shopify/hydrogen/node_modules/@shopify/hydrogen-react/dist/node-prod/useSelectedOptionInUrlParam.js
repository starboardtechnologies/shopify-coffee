require("./_virtual/_rolldown/runtime.js");
const require_getProductOptions = require("./getProductOptions.js");
let react = require("react");
//#region src/useSelectedOptionInUrlParam.tsx
/** @publicDocs */
function useSelectedOptionInUrlParam(selectedOptions) {
	(0, react.useEffect)(() => {
		const optionsSearchParams = new URLSearchParams(require_getProductOptions.mapSelectedProductOptionToObject(selectedOptions || []));
		const currentSearchParams = new URLSearchParams(window.location.search);
		const combinedSearchParams = new URLSearchParams({
			...Object.fromEntries(currentSearchParams),
			...Object.fromEntries(optionsSearchParams)
		});
		if (combinedSearchParams.size > 0) window.history.replaceState({}, "", `${window.location.pathname}?${combinedSearchParams.toString()}`);
	}, [JSON.stringify(selectedOptions)]);
	return null;
}
//#endregion
exports.useSelectedOptionInUrlParam = useSelectedOptionInUrlParam;

//# sourceMappingURL=useSelectedOptionInUrlParam.js.map