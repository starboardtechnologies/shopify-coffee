require("./_virtual/_rolldown/runtime.js");
const require_Image = require("./Image.js");
let react = require("react");
let react_jsx_runtime = require("react/jsx-runtime");
//#region src/Video.tsx
/**
* The `Video` component renders a `video` for the Storefront API's [Video object](https://shopify.dev/api/storefront/reference/products/video).
* @publicDocs
*/
var Video = (0, react.forwardRef)((props, ref) => {
	const { data, previewImageOptions, id = data.id, playsInline = true, controls = true, sourceProps = {}, ...passthroughProps } = props;
	const posterUrl = require_Image.shopifyLoader({
		src: data.previewImage?.url ?? "",
		...previewImageOptions
	});
	if (!data.sources) throw new Error(`<Video/> requires a 'data.sources' array`);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("video", {
		...passthroughProps,
		id,
		playsInline,
		controls,
		poster: posterUrl,
		ref,
		children: data.sources.map((source) => {
			if (!(source?.url && source?.mimeType)) throw new Error(`<Video/> needs 'source.url' and 'source.mimeType'`);
			return /* @__PURE__ */ (0, react.createElement)("source", {
				...sourceProps,
				key: source.url,
				src: source.url,
				type: source.mimeType
			});
		})
	});
});
//#endregion
exports.Video = Video;

//# sourceMappingURL=Video.js.map