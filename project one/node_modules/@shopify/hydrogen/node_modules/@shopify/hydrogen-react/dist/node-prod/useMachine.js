require("./_virtual/_rolldown/runtime.js");
const require_index = require("./node_modules/.pnpm/@xstate_fsm@2.0.0/node_modules/@xstate/fsm/es/index.js");
let react = require("react");
//#region src/useMachine.ts
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? react.useLayoutEffect : react.useEffect;
function useConstant(fn) {
	const ref = (0, react.useRef)();
	if (!ref.current) ref.current = { v: fn() };
	return ref.current.v;
}
function getServiceState(service) {
	let currentValue;
	service.subscribe((state) => currentValue = state).unsubscribe();
	return currentValue;
}
function useMachine(stateMachine, options) {
	const persistedStateRef = (0, react.useRef)();
	const [service, queue] = useConstant(() => {
		const eventQueue = [];
		const svc = require_index.v(require_index.s(stateMachine.config, options ? options : stateMachine._options));
		const originalSend = svc.send;
		svc.send = (event) => {
			if (svc.status === require_index.n.NotStarted) {
				eventQueue.push(event);
				return;
			}
			originalSend(event);
			persistedStateRef.current = svc.state;
		};
		return [svc, eventQueue];
	});
	useIsomorphicLayoutEffect(() => {
		if (options) service._machine._options = options;
	});
	const getSnapshot = (0, react.useCallback)(() => getServiceState(service), [service]);
	const storeSnapshot = (0, react.useSyncExternalStore)((0, react.useCallback)((handleStoreChange) => {
		const { unsubscribe } = service.subscribe(handleStoreChange);
		return unsubscribe;
	}, [service]), getSnapshot, getSnapshot);
	(0, react.useEffect)(() => {
		service.start(persistedStateRef.current);
		queue.forEach(service.send);
		persistedStateRef.current = service.state;
		return () => {
			service.stop();
		};
	}, []);
	return [
		storeSnapshot,
		service.send,
		service
	];
}
//#endregion
exports.useMachine = useMachine;

//# sourceMappingURL=useMachine.js.map