import Command from '@shopify/cli-kit/node/base-command';
import { canRunDoctorRelease } from '@shopify/cli-kit/node/context/local';
import { renderInfo } from '@shopify/cli-kit/node/ui';
class DoctorRelease extends Command {
    async run() {
        if (!canRunDoctorRelease()) {
            return;
        }
        renderInfo({
            headline: 'Shopify CLI Doctor Release.',
            body: [
                'Available doctor-release commands:',
                '',
                '  shopify doctor-release theme -e <environment>   Run all theme command tests',
                '',
                'The -e/--environment flag is required to specify the store configuration.',
                'Use --help with any command for more options.',
            ],
        });
    }
}
DoctorRelease.description = 'Run CLI doctor-release tests';
DoctorRelease.hidden = true;
export default DoctorRelease;
//# sourceMappingURL=doctor-release.js.map